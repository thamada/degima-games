# 変更履歴

> **注意**:
>   本ドキュメントは変更履歴です。日付はdateコマンドで確認して2026-01-23 12:34:55のように年-月-日 時:分:秒のようにします。
>   最も最新のものから順に並べて記入します。

## 2026-06-19 00:44:48

- ゲーム画面のスクロール問題を修正
  - シェル・各ゲーム HTML で `overflow: hidden` を設定
  - Canvas を CSS でウィンドウ内に収まるよう自動縮小（`max-height: calc(100% - 1.25rem)`）
  - ゲーム操作キー（矢印キー・スペース等）で `preventDefault()` を呼び、ページスクロールによる操作妨害を防止

## 2026-06-19 00:38:00

- タブ UI をコンパクトなアンダーライン型に刷新
  - タブバー高さ 32px、ネオン glow 等を廃止して視認性と統一感を改善
- ゲーム HTML のレイアウトを調整（縦中央寄せを廃止し、iframe 内で上部が切れないよう変更）

## 2026-06-19 00:30:00

- タブ付きゲームシェル `src/games/index.html` を追加
  - 「インベーダー」「ポン」を画面上部タブで切り替え可能に
  - 各ゲームを iframe で読み込み、ポンは初回選択時に遅延読み込み
- Electron 起動ファイルを `invaders.html` から `index.html` に変更
- ウィンドウタイトルを「ゲーム」に変更

## 2026-06-19 00:25:00

- `electron/` ディレクトリを `src/electron/` に移動
  - `package.json` の `main` を `src/electron/main.js` に更新
  - `electron-builder` の同梱パスを `src/**/*` に統一
  - `main.js` 内のゲーム HTML 参照パスを新構成に合わせて修正
